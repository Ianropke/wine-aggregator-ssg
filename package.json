{
  "name": "nasty-neutron",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "pip3 install -r python-requirements.txt --break-system-packages || pip3 install -r python-requirements.txt; python3 scripts/pipeline.py && python3 scripts/llmstxt_architect.py && rm -rf .astro && astro build && npx pagefind --site dist && bash scripts/vercel-pagefind-fix.sh",
    "preview": "astro preview",
    "astro": "astro",
    "test": "playwright test"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.2",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/forms": "^0.5.11",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.7",
    "pagefind": "^1.5.2",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "playwright": "^1.61.1"
  }
}
