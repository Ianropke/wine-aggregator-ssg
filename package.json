{
  "name": "nasty-neutron",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "python3 -m venv .venv_build && source .venv_build/bin/activate && pip install -r python-requirements.txt && python scripts/pipeline.py && python scripts/llmstxt_architect.py && astro build && npx pagefind --site dist && bash scripts/vercel-pagefind-fix.sh",
    "preview": "astro preview",
    "astro": "astro",
    "test": "playwright test"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.2",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/forms": "^0.5.11",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.7",
    "pagefind": "^1.5.2",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1"
  }
}
